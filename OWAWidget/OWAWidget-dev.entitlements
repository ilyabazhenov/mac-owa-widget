<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Outbound HTTPS to Exchange servers -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Hardened runtime is on (see Makefile). It would also turn on library
         validation, which refuses to load the ad-hoc signed Sparkle.framework.
         Disabling library validation alone keeps the rest of hardened runtime,
         notably that DYLD environment variables stay ignored. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
