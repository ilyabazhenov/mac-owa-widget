## What's Changed

- Grouped parallel meetings (starting within 5 minutes) into a single reminder for both system notifications and in-app banners, so concurrent calls are visible together.
- Added per-meeting Join actions in the aggregated reminder flow and removed the ambiguous global Join button from the in-app multi-meeting popup.
- Added coverage for reminder clustering and notification payload decoding with new unit tests.
